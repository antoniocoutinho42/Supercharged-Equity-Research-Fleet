# v3.0.0 — reescrita completa: fleet centrado no Analista

Implementação integral do desenho `docs/desenho-workflow-equity-research-fleet-v3.md` (BREAKING).

## Morre (preservado no histórico git)

- As 9 skills v2 (er-processo, er-guardrails, er-dossie, er-dados, er-valuation com engine
  v3.3.0 + cap_check, er-auditoria, er-portfolio, er-relatorio, er-memoria), os 5 agentes,
  `schemas/` inteiro, `scripts/` v2 (pipeline, validar, snapshot, delta, memoria antiga),
  gates G1–G8 com estado formal, claims, entry ladder, âncora dupla e a entrega em PDF.

## Nasce

- **2 agentes**: Analista no loop principal + Data Manager (único subagente, dono do OpenBB).
- **4 skills**: `er-analise` (workflow F0–F11, modo somente-valuation, P2 por materialidade),
  `er-dados-openbb` (mapa real de endpoints, ledger, gaps MATERIAL/NÃO-MATERIAL),
  `er-motor-k3` (cópia congelada da metodologia Justified P/E V2.2.1 — manual, engine,
  regressões, validador; manifest com sha256 por arquivo), `er-relatorio-html` (builder
  determinístico + template 2 abas + espelho k3_engine.js verificado + uPlot 1.6.27 vendorizado
  + checar_relatorio).
- **QC por código**: SUITE PASS (282 checks canônicos), paridade Python↔JS (teste com node +
  pré-verificação no build com recusa + self-test no browser) e checar_relatorio (6 categorias).
- **Memória durável** por código (`scripts/memoria.py`): âncoras de `case.json`/`results.json`,
  síntese e lições cumulativas.

Metodologia congelada V2.2.1 — `formula_version: K3-vF19-2026-08-03 / manual-v2.2.1`,
sha256 da fórmula-fonte `ed51631…3b5420`; sincronização com a skill `justified-pe-valuation`
é manual e por decisão humana (ver README).
