# Equity Research Fleet v3.0.0 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reescrever o plugin da v2.2.0 para a v3.0.0 exatamente como especificado em `docs/desenho-workflow-equity-research-fleet-v3.md` (FONTE DE VERDADE): 2 agentes (Analista no loop + Data Manager), 4 skills novas, motor K3 V2.2.1 copiado e congelado, dados só via OpenBB, entrega em HTML interativo de 2 abas.

**Architecture:** O Analista é o loop principal (skill `er-analise`, fases F0–F11); o Data Manager é o único subagente (skill `er-dados-openbb`). A matemática vive exclusivamente na cópia congelada do engine K3 (`er-motor-k3`, com regression gate) e no espelho JS verificado (`er-relatorio-html`). QC por código: SUITE PASS, paridade Python↔JS, `checar_relatorio.py`. Memória durável por código (`scripts/memoria.py` novo).

**Tech Stack:** Python 3.12 stdlib (engine e builders sem dependências; testes com pytest), JS vanilla no HTML (k3_engine.js copiado + lib de gráficos leve embutida), OpenBB MCP (providers reais: yfinance sem chave; sec p/ US; fmp/intrinio SEM chave nesta máquina).

## Global Constraints

- **Fonte de verdade:** `docs/desenho-workflow-equity-research-fleet-v3.md`. Conflito → o desenho vence. Dúvida não resolvida pelo desenho → perguntar ao Antonio.
- **`justified-pe-valuation` é INTOCÁVEL.** Fonte da cópia (localizada na Fase 0, SUITE PASS 281/282 confirmado):
  `C:\Users\antonio.coutinho\AppData\Roaming\Claude\local-agent-mode-sessions\skills-plugin\a285574f-1119-487a-a43f-55b8af2000fd\d23d90b0-93a5-47f5-bbb9-17f459f7e5f7\skills\justified-pe-valuation\`
  (path gerenciado pelo app; se sumir entre sessões, re-invocar a skill `anthropic-skills:justified-pe-valuation` para obter o novo base dir — o conteúdo é o mesmo).
- **Metodologia congelada V2.2.1:** `valuation_engine.py`, `input_validator.py`, `run_regressions.py`, `K3_Regression_Tests_v2.2.1.json`, manual e `k3_engine.js` copiados **byte a byte** (sha256 conferido). Nenhuma matemática de valuation nova em prosa, Python ou JS.
- **Identidade da metodologia** (registrar no README e no manifest): `formula_version: K3-vF19-2026-08-03 / manual-v2.2.1`; `source_formula_sha256.stored = ed5163103b73a226d47692ad6a9595416ce38ea2e2e9ea0d217072d4243b5420`.
- **Git:** branch `v3.0.0` a partir da main; NUNCA commitar na main; NUNCA push sem ordem. Commits atômicos por task.
- **Convenções de arquivo** (herdadas da v2, mantidas): SKILL.md com frontmatter de exatamente 2 campos (`name` == nome do diretório, `description` no padrão "USE QUANDO … NÃO use para …"); agent .md com 5 seções (`Identidade`, `Fronteiras duras`, `Skill obrigatória`, `Insumos e entregáveis`, `Retorno` com "NO MÁXIMO 10 linhas") e ≤4096 bytes; PT-BR direto; narrativa PT-BR com nomes canônicos dos 16 inputs em inglês.
- **Invariantes da Seção 9 do desenho** valem para todo conteúdo escrito (hurdle sem default; market-implied nunca recalibra; gaps MATERIAL perguntam / NÃO-MATERIAL degradam com nota; dados financeiros só OpenBB via DM com exceção única do snapshot F1; respostas de grill-me são evidência; memória gerada por código).
- **Node.js:** não está instalado nesta máquina. Testes de paridade JS devem fazer `pytest.skip` limpo sem node (no CI ubuntu há node e eles RODAM). Se o Antonio aprovar instalar Node local (winget), o gate de paridade roda também localmente.
- Encoding: todo arquivo novo em UTF-8; em subprocess de teste usar `encoding="utf-8"` (lição v2: cp1252 no Windows).
- `pyproject.toml` mantém `testpaths=["tests"]` (impede o pytest de coletar scripts standalone das skills).

---

## Decisões e correções incorporadas (aprovação do Antonio, 2026-08-05)

1. **Execução:** subagent-driven nas Tasks 1, 2, 3, 6 e 8; inline nas Tasks 4, 5 e 9. Task 7 não foi atribuída na aprovação → inline (prosa de julgamento, mesma família das 4/5; registrar como decisão do executor). Sem checkpoints com o Antonio entre tasks; entrega única ao final. Só interromper por contradição real com o desenho ou bloqueio intransponível.
2. **Fonte da cópia confirmada** (path do app). A partir da Task 2, a cópia no plugin é a referência canônica. `manifest_copia.json` registra: path de origem, data da cópia e contagem observada da suíte.
3. **Node.js:** instalar via winget (aprovado). Paridade Python↔JS roda LOCALMENTE (é 1 dos 3 mecanismos de QC da v3). CI: garantir node no job (`actions/setup-node@v4`).
4. **Gráficos:** vendorizar **uPlot** em versão fixa (MIT), versão + sha256 registrados em `manifest_copia.json`, embutido no HTML (zero rede). Como canvas não expõe números no DOM, `checar_relatorio.py` valida séries lendo `__REPORT_DATA__` e conferindo contra `dados/` e `results.json`.
5. **API do engine VERIFICADA no fonte real** (nada inventado; engine jamais editado): `value_case(case) -> dict` com `engine_version`, `implementation_version`, `engine_fingerprint_ok`, `scenarios{<nome>}`; por cenário: `computed`, `route`, `blocks`, `pb_blocks`, `K3_trailing_PE`, `forward_PE`, `implied_PB0`, `K_PB`, `equity_value`, `value_per_share`, `vs_market{price, upside}`, `warnings`, `validation`, `sanity_notes`, `funding`, `label`; `sensitivity_1d(case, scenario, param, values)`, `sensitivity_2d(case, scenario, p1, values1, p2, values2)`, `solve_market_implied(case, scenario, param)`, `js_parity_vectors()`, `ENGINE_VERSION`, `_FINGERPRINT_INPUTS`. Upside fica em `vs_market.upside` (corrigido nos testes). Helpers extras só em arquivo NOVO do plugin.
6. **Harness de paridade:** VERIFICADO — `k3_engine.js` TEM CLI node (`node k3_engine.js --selftest < vectors.json`, linhas 344-348). Sem wrapper; `tests/test_parity_js.py` usa o CLI do arquivo copiado, intocado.
7. **Autocontenção:** proibir **carregamento externo de recurso** (`src=` http(s), `<link rel="stylesheet" href=` http(s), `@import` http(s), `fetch(`, `XMLHttpRequest`, `import(` http(s)) — `<a href>` de citação é PERMITIDO (aba 2 tem seção de fontes). Mesma regra no builder e no `checar_relatorio.py`.
8. **Contagem da suíte:** CONFIRMADO granularidade/patch, não divergência: "259" do SKILL.md é contagem antiga (seções 1–16 + paridade JS ativa, pré-patch); o patch de implementação 1.1.0 adicionou a seção 18 (21 checks de funding α-adjusted). `formula_version` idêntica (`K3-vF19-2026-08-03 / manual-v2.2.1`), fingerprint OK, JSON canônico o mesmo. Registrar contagem observada (com node) no manifest.
9. **Horizonte de dados (decidido):** aceitar a janela disponível, degradando — "só OpenBB" intacto. (a) gráficos "10 anos" → "janela máxima disponível", com janela efetiva + nota exibidas NO gráfico; (b) horizonte curto = gap NÃO-MATERIAL (ledger + Limitações; nunca pergunta, nunca bloqueia); (c) `er-analise` manda declarar na justificativa de ROE2/g2/CAP que série curta enfraquece a âncora histórica; (d) preço longo + fundamentals curtos → construir o construível e declarar o corte; (e) mapa de endpoints parametrizado por `provider` (troca sem refactor se chave for configurada depois).
10. **CI:** `pip install pytest pyyaml`.
11. **Task 9 sem interação:** executor escolhe ticker B3 que estresse cobertura OpenBB + regra de gaps, declara escolha e motivo; defaults da assunção 10 (modo completo, sem hurdle, sem checkpoint); grill-me simulado com respostas declaradas explicitamente como SUPOSIÇÕES no relatório.

---

### Task 1: Limpeza do repo + bump 3.0.0 + CI (item 1 da Seção 12)

**Files:**
- Delete: `skills/` (as 9 skills v2 inteiras), `agents/` (5 .md), `schemas/` (7 schemas), `scripts/pipeline.py`, `scripts/validar.py`, `scripts/snapshot.py`, `scripts/delta.py`, `scripts/memoria.py`, `scripts/__pycache__/`, `tests/` (todos os test_*.py e `fixtures/`), `dist/equity-research-fleet-v1.0.0.zip`, `.pytest_cache/`
- Keep: `docs/` inteiro (histórico; inclui `docs/fontes/`), `.superpowers/`
- Modify: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `.gitignore`
- Create: `tests/test_manifesto.py`

**Interfaces:**
- Produces: repo v3 vazio de v2, versão 3.0.0, CI verde com suíte mínima. Tasks 2+ criam `skills/er-*` novos e acumulam testes em `tests/`.

- [ ] **Step 1:** `git checkout -b v3.0.0` a partir da main; primeiro commit: adicionar o desenho (hoje untracked): `git add docs/desenho-workflow-equity-research-fleet-v3.md && git commit -m "docs: desenho v3.0.0 (fonte de verdade)"`.
- [ ] **Step 2:** Escrever `tests/test_manifesto.py` ANTES da limpeza (falha até o bump):

```python
import json
from pathlib import Path

RAIZ = Path(__file__).resolve().parent.parent

def test_plugin_json_v3():
    p = json.loads((RAIZ / ".claude-plugin" / "plugin.json").read_text(encoding="utf-8"))
    assert p["version"] == "3.0.0"
    assert p["name"] == "equity-research-fleet"
    assert p["description"].strip()

def test_marketplace_json_v3():
    m = json.loads((RAIZ / ".claude-plugin" / "marketplace.json").read_text(encoding="utf-8"))
    assert m["metadata"]["version"] == "3.0.0"
    assert m["plugins"][0]["version"] == "3.0.0"

def test_v2_removida():
    for morto in ["schemas", "scripts/pipeline.py", "scripts/validar.py", "scripts/snapshot.py",
                  "scripts/delta.py", "skills/er-processo", "skills/er-valuation",
                  "skills/er-relatorio", "skills/er-auditoria", "skills/er-portfolio",
                  "skills/er-guardrails", "skills/er-dossie", "skills/er-dados",
                  "skills/er-memoria", "agents/analista.md", "agents/modelador.md",
                  "agents/auditor.md", "agents/portfolio-manager.md", "agents/redator.md"]:
        assert not (RAIZ / morto).exists(), f"resto v2: {morto}"
```

- [ ] **Step 3:** Rodar `python -m pytest tests/test_manifesto.py -q` → FAIL (versão 2.2.0 e v2 presente).
- [ ] **Step 4:** Executar a limpeza (git rm dos paths listados em Delete), bump `plugin.json` e `marketplace.json` para `3.0.0` com description nova: "Fleet de equity research v3: Analista + Data Manager, motor K3 V2.2.1 congelado, dados via OpenBB, relatório HTML interativo de 2 abas".
- [ ] **Step 5:** Reescrever `ci.yml`: python 3.12; `pip install pytest`; passos: `python -m pytest tests/ -q` + sanity inline do plugin.json (manter). Remover os passos do golden vrsk e do cap_check. Reescrever `release.yml` igual (mantendo validação tag==version, ZIP com exclusões atuais, corpo de `docs/releases/<tag>.md`).
- [ ] **Step 6:** `.gitignore`: remover `saida_*/`; adicionar `analises/`. Rodar `python -m pytest tests/ -q` → PASS (só test_manifesto).
- [ ] **Step 7:** Commit: `chore!: v3.0.0 — remove processo v2 (skills, agents, schemas, scripts, testes); bump manifesto e CI`.

### Task 2: `er-motor-k3` — cópia congelada + SKILL.md (item 2)

**Files:**
- Create: `skills/er-motor-k3/SKILL.md`, `skills/er-motor-k3/references/Justified_PE_AI_Manual_v2.2.1.md` (cópia), `skills/er-motor-k3/references/K3_Regression_Tests_v2.2.1.json` (cópia), `skills/er-motor-k3/scripts/valuation_engine.py` (cópia), `skills/er-motor-k3/scripts/input_validator.py` (cópia), `skills/er-motor-k3/scripts/run_regressions.py` (cópia), `skills/er-motor-k3/manifest_copia.json`
- Test: `tests/test_motor_k3.py`

**Interfaces:**
- Produces: `python skills/er-motor-k3/scripts/run_regressions.py` → SUITE PASS; `import valuation_engine as ve` (via sys.path no dir scripts) expõe `value_case(case: dict) -> dict`, `solve_market_implied(case, scenario, param)`, `sensitivity_2d(...)`, `js_parity_vectors()`, `ENGINE_VERSION`. `case.json` no schema da skill standalone (company, currency, valuation_date, market{price_per_share, shares_diluted_t0}, scenarios{<nome>{inputs{16}, scale{NI0|Book0}, weight?, label_mode?}}).
- Consumes: fonte da cópia (Global Constraints).

- [ ] **Step 1:** Escrever `tests/test_motor_k3.py` (falha até a cópia existir):

```python
import hashlib, json, subprocess, sys
from pathlib import Path

RAIZ = Path(__file__).resolve().parent.parent
MK3 = RAIZ / "skills" / "er-motor-k3"

def _sha(p):
    return hashlib.sha256(p.read_bytes()).hexdigest()

def test_manifest_congelado():
    man = json.loads((MK3 / "manifest_copia.json").read_text(encoding="utf-8"))
    assert man["formula_version"] == "K3-vF19-2026-08-03 / manual-v2.2.1"
    assert man["source_formula_sha256_stored"] == "ed5163103b73a226d47692ad6a9595416ce38ea2e2e9ea0d217072d4243b5420"
    for rel, sha in man["arquivos"].items():
        assert _sha(MK3 / rel) == sha, f"arquivo copiado alterado: {rel}"

def test_regressao_suite_pass():
    r = subprocess.run([sys.executable, str(MK3 / "scripts" / "run_regressions.py")],
                       capture_output=True, text=True, encoding="utf-8", timeout=300)
    assert r.returncode == 0, r.stdout + r.stderr
    assert "SUITE PASS" in r.stdout

def test_skill_md_sem_workflow_standalone():
    texto = (MK3 / "SKILL.md").read_text(encoding="utf-8")
    assert texto.startswith("---")
    for proibido in ["Markdown ou", "grill me", "grill-me", "Mandatory opening interaction"]:
        assert proibido not in texto, f"workflow standalone vazou: {proibido}"
    for obrigatorio in ["16", "run_regressions.py", "SUITE PASS", "case.json", "arquétipo"]:
        assert obrigatorio in texto
```

- [ ] **Step 2:** Rodar → FAIL. Copiar os 5 arquivos byte a byte (`Copy-Item`), conferir `Get-FileHash` origem==destino, gerar `manifest_copia.json`:

```json
{
  "formula_version": "K3-vF19-2026-08-03 / manual-v2.2.1",
  "source_formula_sha256_stored": "ed5163103b73a226d47692ad6a9595416ce38ea2e2e9ea0d217072d4243b5420",
  "copiado_de": "skill justified-pe-valuation (INTOCADA)",
  "copiado_em": "<data>",
  "arquivos": {"references/...": "<sha256>", "scripts/...": "<sha256>"}
}
```

- [ ] **Step 3:** Rodar a suíte DENTRO do plugin → SUITE PASS (a seção 17 JS fará skip: k3_engine.js fica na `er-relatorio-html`; paridade é testada na Task 5).
- [ ] **Step 4:** Escrever `SKILL.md` SEM workflow standalone. Conteúdo (Seção 2.2 do desenho): frontmatter (`name: er-motor-k3`; description "USE QUANDO precisar da metodologia K3 V2.2.1: relações financeiras, calibração dos 16 inputs, arquétipos/perfis, rodar o engine/regressões… NÃO use para coleta de dados, relatório ou workflow de análise"); mapa dos canonical sources (manual §§, JSON); os 16 inputs; ordem de calibração §4.0; política de Ke §6 (resumo + ponteiro); arquétipos §7 (ponteiro por família); como rodar o engine (case.json + CLI `value|sens|implied` + `run_regressions.py`); contrato de inputs (INVALID/REVIEW/boundaries); disciplina de output (citar por chave; funding α-adjusted; labels hurdle/reverse-Ke/probability-weighted); regression gate. SEM pergunta de formato, SEM grill-me, SEM etapas de pesquisa.
- [ ] **Step 5:** `python -m pytest tests/ -q` → PASS. Commit: `feat(er-motor-k3): cópia congelada K3 V2.2.1 (SUITE PASS) + SKILL.md sem workflow standalone`.

### Task 3: `er-dados-openbb` + `agents/data-manager.md` (item 3)

**Files:**
- Create: `skills/er-dados-openbb/SKILL.md`, `skills/er-dados-openbb/references/endpoints.md`, `skills/er-dados-openbb/references/ledger-e-gaps.md`, `agents/data-manager.md`
- Test: `tests/test_dados_openbb.py`

**Interfaces:**
- Produces: doutrina que o DM segue; formatos: `analises/<TICKER>/dados/<categoria>.json|csv`, `dados/ledger.md` (tabela: arquivo | endpoint | parâmetros | provider | data da chamada | cobertura | gap MATERIAL/NÃO-MATERIAL), `dados/pedidos.md` (log pedido→resposta).
- Consumes: mapa real de endpoints da Fase 0 (validado neste task com chamadas extras).

- [ ] **Step 1:** Validar o mapa endpoint→categoria com chamadas reais adicionais num ticker B3 (yfinance: `equity_fundamental_balance`, `_cash`, `_dividends`, `_historical_eps`, `equity_price_historical`, `equity_fundamental_metrics`; anotar horizonte de cada série — yfinance entrega ~4-5 anos de demonstrações, não 10: registrar como limitação estrutural na doutrina, com a regra de declarar gap NÃO-MATERIAL de horizonte quando o gráfico pedir 10a).
- [ ] **Step 2:** Escrever `tests/test_dados_openbb.py` (forma e âncoras de conteúdo):

```python
from pathlib import Path
import yaml

RAIZ = Path(__file__).resolve().parent.parent

def _frontmatter(p):
    texto = p.read_text(encoding="utf-8")
    assert texto.startswith("---")
    fm = yaml.safe_load(texto.split("---")[1])
    assert set(fm) == {"name", "description"}
    return fm, texto

def test_skill_dados():
    fm, texto = _frontmatter(RAIZ / "skills" / "er-dados-openbb" / "SKILL.md")
    assert fm["name"] == "er-dados-openbb"
    for ancora in ["MATERIAL", "NÃO-MATERIAL", "ledger", "openbb", "yfinance",
                   "24h", "proveniência", "pedidos.md"]:
        assert ancora.lower() in texto.lower(), ancora

def test_agent_data_manager():
    p = RAIZ / "agents" / "data-manager.md"
    fm, texto = _frontmatter(p)
    assert fm["name"] == "data-manager"
    assert p.stat().st_size <= 4096
    for secao in ["## 1. Identidade", "## 2. Fronteiras duras", "## 3. Skill obrigatória",
                  "## 4. Insumos e entregáveis", "## 5. Retorno"]:
        assert secao in texto
    assert "er-dados-openbb" in texto and "10 linhas" in texto
```

(pyyaml volta como dependência de teste: adicionar ao `pip install` do CI.)
- [ ] **Step 3:** Rodar → FAIL. Escrever `references/endpoints.md`: tabela categoria → tool OpenBB → provider utilizável → parâmetros → horizonte observado → gap típico, cobrindo as 9 categorias do F4 (demonstrações 10a, preço/múltiplos, EPS, dividendos/splits/buybacks, estimates/consenso, ownership/insiders, segmentos/geografia, setoriais/commodities via `commodity_price_spot`/`economy_fred_series`/`index_price_historical`/`currency_price_historical`, pares via `equity_compare_peers`). Registrar: `provider` é parâmetro obrigatório; fmp/intrinio SEM chave nesta máquina (erro real documentado); B3 = sufixo `.SA` no yfinance; `sec` só US.
- [ ] **Step 4:** Escrever `references/ledger-e-gaps.md`: formato do ledger; validade de preço (>24h útil = VENCIDO, recoletar); anti-cascata ("nenhum 'indisponível' sem a chamada que o sustenta; nunca herdar ausência"); classificação de gap: MATERIAL = bloqueia input crítico do valuation ou a escala (→ Analista pergunta ao usuário, que pode fornecer manualmente); NÃO-MATERIAL = degrada gráfico/contexto (→ nota de limitação e segue). Nunca inventar, nunca silenciar.
- [ ] **Step 5:** Escrever `SKILL.md` (doutrina de coleta: papel do DM, categorias, formatos de arquivo em `dados/`, proatividade — sugerir séries úteis não pedidas, ex. commodity do perfil —, iteração com o Analista via `pedidos.md`) e `agents/data-manager.md` (5 seções, ≤4096 bytes; Identidade: único dono das chamadas OpenBB, exceção única F1; Fronteiras: não interpreta, não calibra, não escreve análise; Retorno: ≤10 linhas com gaps classificados, nunca colar conteúdo de arquivo).
- [ ] **Step 6:** `python -m pytest tests/ -q` → PASS. Commit: `feat(er-dados-openbb): doutrina de coleta OpenBB + agente data-manager`.

### Task 4: `er-relatorio-html` — assets + template 2 abas (item 4, parte 1)

**Files:**
- Create: `skills/er-relatorio-html/assets/k3_engine.js` (cópia byte a byte; sha no manifest da Task 2 — adicionar entrada), `skills/er-relatorio-html/assets/template_relatorio.html`, `skills/er-relatorio-html/assets/uPlot.iife.min.js` + `assets/uPlot.min.css` (vendor uPlot versão fixa, MIT; versão+sha256 no manifest — DECIDIDO), `skills/er-relatorio-html/assets/charts.js` (adaptador fino nosso sobre uPlot: séries, overlays de premissas, nota de janela efetiva)
- Test: `tests/test_parity_js.py`

**Interfaces:**
- Produces: template com placeholders `__PAGE_TITLE__`, `__K3_ENGINE_JS__`, `__CHARTS_JS__`, `__REPORT_DATA__` (JSON único com: header comum, payload por cenário com `python_expected`, presets 2D, market_implied, `parity_vectors`, conteúdo da aba 2, séries dos gráficos, ledger de gaps); aba 1 completa (tabs de cenário, inputs editáveis com highlight/reset, recálculo ao vivo, sensibilidade 1D ao vivo + 2D precomputada, decomposição em blocos + bridge, market-implied, comparação com preço, self-test de paridade no load com banner); aba 2 vazia ainda (Task 5 preenche via builder).
- Consumes: `k3_engine.js` (API: `K3Engine.valueScenario(inputs, NI0, Book0)` etc. — espelho do Python), template da skill standalone como referência de porte (NUNCA editado na origem).

- [ ] **Step 1:** Escrever `tests/test_parity_js.py`:

```python
import json, shutil, subprocess, sys
from pathlib import Path
import pytest

RAIZ = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(RAIZ / "skills" / "er-motor-k3" / "scripts"))

def test_paridade_python_js():
    node = shutil.which("node")
    if not node:
        pytest.skip("node indisponível nesta máquina (roda no CI)")
    import valuation_engine as ve
    js = RAIZ / "skills" / "er-relatorio-html" / "assets" / "k3_engine.js"
    vetores = ve.js_parity_vectors()
    r = subprocess.run([node, str(js), "--selftest"], input=json.dumps(vetores),
                       capture_output=True, text=True, encoding="utf-8", timeout=60)
    res = json.loads(r.stdout)
    assert res["pass"], res.get("failures")
```

- [ ] **Step 2:** Copiar `k3_engine.js` byte a byte; atualizar `manifest_copia.json` (Task 2) com a entrada `../er-relatorio-html/assets/k3_engine.js` e seu sha256; rodar `pytest tests/test_parity_js.py -q` → SKIP local (PASS no CI).
- [ ] **Step 3:** Implementar `charts.js` conforme decisão do Antonio (linha/barra, tooltip, zoom simples, overlay de linhas horizontais para premissas — assunção 7 do desenho —, tema autocontido, sem rede).
- [ ] **Step 4:** Escrever `template_relatorio.html`: navegação de 2 abas top-level ("Valuation" / "Análise"), cabeçalho comum (empresa, ticker, data, preço usado com data e fonte, síntese com fair value base/upside/faixa bear–bull/hurdle-conditioned quando houver, badge de paridade), aba 1 portando a mecânica interativa do template da skill standalone adaptada ao layout novo (mesma disciplina: matemática SÓ via `K3Engine`; banner vermelho em divergência do self-test). Sensibilidade 1D recalcula ao vivo com as premissas correntes; 2D vem precomputada do builder.
- [ ] **Step 5:** Smoke manual: montar um HTML de fixture com o base case do fingerprint (`ve._FINGERPRINT_INPUTS`, NI0=5.75), abrir no browser interno, verificar banner verde + edição recalculando. Commit: `feat(er-relatorio-html): template 2 abas + espelho JS + charts embutidos`.

### Task 5: `er-relatorio-html` — `build_report.py` + aba 2 (item 4, parte 2)

**Files:**
- Create: `skills/er-relatorio-html/build_report.py`, `skills/er-relatorio-html/SKILL.md`
- Test: `tests/test_build_report.py`

**Interfaces:**
- Produces: CLI `python skills/er-relatorio-html/build_report.py <ns> --out <ns>/relatorio/relatorio_<TICKER>.html [--modo completo|valuation]`, onde `<ns>` = `analises/<TICKER>/` contendo `case.json`, `saida/results.json` (gerado pelo engine na F8), `analise.json` (conteúdo da aba 2, produzido pelo Analista: positives/negatives, sumário executivo, blocos HTML por seção, interpretações do market-implied, gaps do ledger) e `dados/*.json` (séries dos gráficos). Recusa emitir se: regressão falha, fingerprint falha, paridade node falha (quando node existe), número da aba 2 não bate com `results.json`.
- Produces (aba 2): seções na ordem da Seção 8 do desenho: 1. Positives/Negatives, 2. Sumário executivo, 3. Perfil e qualitativa, 4. Financeira histórica com gráficos obrigatórios (ROE 10a, EPS+CAGR, endividamento, P/L histórico, crescimento anual+CAGR acumulado, específicos do perfil), 5. Calibração 16 inputs × cenários + racional, 6. Riscos, 7. What Is the Market Pricing? narrativo, 8. Limitações de dados e fontes. Gráfico sem série completa → degrada com nota explícita. Modo `valuation`: só aba 1 + seção de premissas.
- Consumes: `ve.value_case`, `ve.solve_market_implied`, `ve.sensitivity_2d`, `ve.js_parity_vectors` (Task 2); template e assets (Task 4).

- [ ] **Step 1:** Escrever `tests/test_build_report.py` com fixture sintética (case do fingerprint + `analise.json` mínimo + séries fictícias):

```python
import json, re, subprocess, sys
from pathlib import Path
import pytest

RAIZ = Path(__file__).resolve().parent.parent
BUILD = RAIZ / "skills" / "er-relatorio-html" / "build_report.py"

@pytest.fixture
def ns(tmp_path):
    # monta analises/TST3/ com case.json (fingerprint base), roda o engine p/ results.json,
    # escreve analise.json mínimo e series de gráfico fictícias em dados/
    ...

def test_html_autocontido_e_numeros_batem(ns):
    r = subprocess.run([sys.executable, str(BUILD), str(ns), "--out", str(ns / "r.html")],
                       capture_output=True, text=True, encoding="utf-8")
    assert r.returncode == 0, r.stdout + r.stderr
    html = (ns / "r.html").read_text(encoding="utf-8")
    # autocontenção = nenhum CARREGAMENTO externo de recurso; <a href> de citação é permitido
    import re as _re
    assert not _re.search(r'(?:src|srcset)\s*=\s*["\']https?://', html)
    assert not _re.search(r'<link[^>]+href\s*=\s*["\']https?://', html)
    assert not _re.search(r'@import\s+(?:url\()?["\']?https?://', html)
    assert "XMLHttpRequest" not in html
    assert not _re.search(r'\bfetch\s*\(|\bimport\s*\(\s*["\']https?://', html)
    results = json.loads((ns / "saida" / "results.json").read_text(encoding="utf-8"))
    k3 = results["scenarios"]["base"]["K3_trailing_PE"]
    assert f"{k3:.2f}" in html  # número citado existe e bate

def test_recusa_paridade_divergente(ns, tmp_path, monkeypatch):
    # aponta o builder para um k3_engine.js adulterado (cópia com constante trocada)
    # espera returncode != 0 e mensagem de recusa
    ...

def test_modo_valuation_sem_aba2(ns):
    ...
```

(Os corpos `...` são preenchidos na implementação — os NOMES e contratos acima são fixos.)
- [ ] **Step 2:** Rodar → FAIL. Implementar `build_report.py` reutilizando os padrões do `compor.py` v2 (histórico git): LOG número→chave por construção (toda cifra da aba 2 inserida via função `n(valor, chave)` que registra o par), degradação com nota padronizada, idempotência. Paridade: reproduzir a pré-verificação node do `build_html_report.py` da skill standalone (sem node → aviso e segue, banner do browser cobre; com node → divergência = recusa).
- [ ] **Step 3:** Escrever `SKILL.md` (`name: er-relatorio-html`; comandos, contrato do `analise.json`, spec das 2 abas, regra dura: builder é o ÚNICO caminho de emissão; proibido editar HTML à mão).
- [ ] **Step 4:** `python -m pytest tests/ -q` → PASS. Commit: `feat(er-relatorio-html): build_report determinístico com recusa por divergência`.

### Task 6: `checar_relatorio.py` (item 4, parte 3)

**Files:**
- Create: `skills/er-relatorio-html/checar_relatorio.py`
- Test: `tests/test_checar_relatorio.py`

**Interfaces:**
- Produces: CLI `python skills/er-relatorio-html/checar_relatorio.py <ns>` — exit 0 limpo / exit 1 com lista de violações: número citado no HTML sem correspondência em `results.json` (usa o LOG embutido pelo builder num bloco `<script type="application/json" id="log-consistencia">`), ausência do self-test de paridade, referência externa de rede, aba obrigatória ausente, gráfico obrigatório ausente sem nota de degradação.

- [ ] **Step 1:** Escrever `tests/test_checar_relatorio.py`: caso limpo (fixture da Task 5) → exit 0; HTML adulterado (número trocado à mão; script de paridade removido; `<img src="https://...">` injetado) → exit 1 com a violação nomeada.
- [ ] **Step 2:** Rodar → FAIL. Implementar. Rodar → PASS.
- [ ] **Step 3:** Commit: `feat(er-relatorio-html): checar_relatorio como QC final por código`.

### Task 7: `er-analise` — workflow F0–F11 (item 5)

**Files:**
- Create: `skills/er-analise/SKILL.md`, `skills/er-analise/references/fases.md`, `skills/er-analise/references/qualitativa-e-financeira.md`, `skills/er-analise/references/memoria-e-p2.md`
- Test: `tests/test_er_analise.py`

**Interfaces:**
- Consumes: `er-motor-k3` (F3/F7/F8), `er-dados-openbb`/data-manager (F4), `er-relatorio-html` (F10), `scripts/memoria.py` (F11 — Task 8 entrega; referenciar pelo caminho definitivo `scripts/memoria.py`).
- Produces: workflow master. SKILL.md enxuto (trigger + mapa de fases + regras duras + modos); detalhe em references.

- [ ] **Step 1:** Escrever `tests/test_er_analise.py` (forma + âncoras das regras duras):

```python
def test_skill_analise():
    fm, texto = _frontmatter(RAIZ / "skills" / "er-analise" / "SKILL.md")
    assert fm["name"] == "er-analise"
    for ancora in ["F0", "F11", "somente valuation", "hurdle", "nunca", "default",
                   "notas.md", "grill-me", "MATERIAL", "market-implied", "memória"]:
        assert ancora.lower() in texto.lower(), ancora
    # invariantes textuais que NÃO podem faltar (Seção 9):
    for regra in ["nunca tem default", "evidência a verificar", "nunca recalibra"]:
        assert regra.lower() in texto.lower(), regra
```

- [ ] **Step 2:** Rodar → FAIL. Escrever SKILL.md: frontmatter (description no padrão do `er-processo` v2: "USE SEMPRE, ANTES de qualquer resposta, quando o pedido envolver analisar empresa/ação com ticker, valuation, P2 ou pergunta sobre ticker já analisado… NÃO use para sourcing sem ticker ou carteira → portfolio-construction"); corpo: os dois modos; tabela F0–F11 (uma linha por fase: objetivo, entrada, saída, checkpoint); regras duras (Seção 9 verbatim adaptada); comportamento desatendido (defaults declarados: modo completo, sem hurdle, sem checkpoint — assunção 10); `notas.md` obrigatório por fase; despacho do DM em background (F4) com sincronização em F6.
- [ ] **Step 3:** `references/fases.md`: detalhe por fase (F1 briefing 1 página + única chamada OpenBB permitida ao Analista; F2 grill-me na ordem fixa modo→hurdle→validação→específicas, respostas em `01_grill_me.md`; F3 perfil/arquétipo via manual §7 → lista de dados, riscos de calibração e gráficos específicos; F5/F6 conectar qualitativo↔números e gerar insights novos; F7 ordem canônica §4.0, nunca calibrar contra preço; F8 sequência SUITE PASS → value → resolver INVALID/justificar REVIEW → sens 1D/2D nos drivers reais → congelar → implied → hurdle rotulada; F9 checkpoint opcional com leitura market-implied por premissa divergente; F10 build + checar; F11 memória). Modo somente-valuation (Seção 6): F4 essencial, F5–F6 → pesquisa dirigida direto em `calibracao.md`, HTML reduzido.
- [ ] **Step 4:** `references/qualitativa-e-financeira.md`: porte curado dos 8 pilares + moat-duração + roteiro de reconstrução histórica (DuPont, margens/giro, alavancagem/cobertura, conversão de caixa e FCFE pelas relações do manual, EPS/CAGR, payout/recompras, P/L histórico) — do histórico git da v2, sem claims e sem linguagem de gates. `references/memoria-e-p2.md`: protocolo de leitura (memória antes de workspace), pergunta pontual, P2 por materialidade com seção "O que mudou", regra de correção na fonte + regenerar.
- [ ] **Step 5:** `python -m pytest tests/ -q` → PASS. Commit: `feat(er-analise): workflow F0–F11, modos, invariantes e P2`.

### Task 8: `scripts/memoria.py` adaptado (item 6)

**Files:**
- Create: `scripts/memoria.py` (reescrita sobre a lógica v2 do histórico git)
- Test: `tests/test_memoria_v3.py`

**Interfaces:**
- Produces: CLI `python scripts/memoria.py analises/<TICKER> [--saida analises/_memoria] [--sintese arq.md] [--licoes arq.md]` → `analises/_memoria/<TICKER>.md`. Seções: 1. Cabeçalho (ticker, `valuation_date`, moeda, preço usado, `ENGINE_VERSION`); 2. Âncoras numéricas (tabela declarativa `_ANCORAS` lida de `saida/results.json` + `case.json` via `_get_path`: fair value/ação base, faixa bear–bull, upside vs preço, K3 base, hurdle-conditioned se existir, cenários e pesos); 3. Síntese e decisão (verbatim de `--sintese`; regenerável); 4. Lições reutilizáveis (cumulativa, `### Atualização YYYY-MM-DD`, NUNCA apagadas em regeneração). Escrita atômica; aviso >150 linhas.
- Consumes: `case.json`/`results.json` da Task 2 (chaves reais do engine: `scenarios.<n>.value_per_share`, `equity_value`, `K3_trailing_PE`, `upside`, `label_mode`).

- [ ] **Step 1:** Escrever `tests/test_memoria_v3.py`: fixture = case fingerprint rodado pelo engine copiado (determinístico); gera nota → asserts de estrutura e valores (o número da âncora == o valor da chave no results.json formatado); regeneração preserva lições; `--licoes` apensa sem duplicar; erro claro sem `results.json`.
- [ ] **Step 2:** Rodar → FAIL. Implementar portando `_get_path`/`_ANCORAS`/append de lições da v2 (histórico git) com as fontes novas. Rodar → PASS.
- [ ] **Step 3:** Commit: `feat(memoria): nota durável por ticker a partir de case.json/results.json`.

### Task 9: README novo + teste ponta a ponta (item 7)

**Files:**
- Create: `README.md` (do zero), `docs/releases/v3.0.0.md`
- Modify: `.github/workflows/ci.yml` (se surgiram dependências de teste), `manifest_copia.json` (se faltou entrada)
- Test: E2E real + `python -m pytest tests/ -q` inteira

**Interfaces:**
- Consumes: TODAS as tasks anteriores.

- [ ] **Step 1:** README do zero: o que é a v3 (2 agentes, 4 skills, fluxo F0–F11 resumido, workspace `analises/<TICKER>/`), invariantes, **registro da metodologia**: "Motor: cópia congelada da skill `justified-pe-valuation` — metodologia V2.2.1, `formula_version K3-vF19-2026-08-03 / manual-v2.2.1`, `source_formula_sha256 ed51631…3b5420` (íntegra em `manifest_copia.json`); qualquer evolução da metodologia é replicada MANUALMENTE nos dois lugares, por decisão humana" — e a disciplina de sincronização; como rodar testes; dependências (Python 3.12; pytest+pyyaml p/ testes; node opcional p/ gate local de paridade).
- [ ] **Step 2:** PERGUNTAR AO ANTONIO o ticker B3 do aceite (estressar cobertura OpenBB + regra de gaps). Rodar a análise completa real ponta a ponta seguindo `er-analise` (grill-me com o Antonio, DM real coletando via OpenBB, F5–F11 completos).
- [ ] **Step 3:** Aceite (todos obrigatórios): SUITE PASS; banner de paridade verde no HTML aberto; HTML autocontido offline com as duas abas funcionando (cenários editáveis com recálculo ao vivo, sensibilidades 1D/2D, market-implied; aba 2 com Positives/Negatives no topo, 5 gráficos obrigatórios + específicos); `checar_relatorio.py` exit 0; memória gerada por código; `python -m pytest tests/ -q` inteira verde.
- [ ] **Step 4:** Entregar: HTML gerado, resumo por etapa, lista de desvios do desenho (meta: vazia). Commit final: `docs: README v3 + release notes`. NÃO fazer push nem tag sem ordem.

---

## Checkpoints com o Antonio

1. **Aprovação deste plano** (antes da Task 1) + 3 decisões: fonte da cópia (path do app) OK?; instalar Node.js local?; lib de gráficos (vendor uPlot ~48KB MIT, requer 1 download aprovado, vs módulo SVG próprio sem dependência).
2. Fim de cada task: checkpoint curto (o que fechou, evidência, próximo passo).
3. Task 9: escolha do ticker B3 + grill-me real.

## Self-Review (executado)

- Cobertura da spec: Seção 3 (sai/entra) → Tasks 1–8; Seção 2 (componentes) → Tasks 2–7; Seção 4 (workspace) → Tasks 5, 7, 8; Seções 5–7 (fluxos) → Task 7; Seção 8 (HTML) → Tasks 4–6; Seção 9 (invariantes) → Global Constraints + âncoras de teste; Seções 10–11 → decisões refletidas nas tasks; Seção 12 → ordem das tasks. Sem gap identificado.
- Tipos/nomes consistentes entre tasks (ve.value_case, results.json, analises/<TICKER>/, manifest_copia.json) — conferido.
- Corpos `...` em 2 fixtures de teste são intencionais (preenchidos no TDD da task, contratos fixados no plano).
