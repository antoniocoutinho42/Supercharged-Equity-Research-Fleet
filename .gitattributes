# Cópia congelada K3 V2.2.1 (byte a byte, sha256 em manifest_copia.json):
# nunca converter EOL nestes paths — qualquer conversão quebra o congelamento.
skills/er-motor-k3/references/** -text
skills/er-motor-k3/scripts/** -text
skills/er-relatorio-html/assets/k3_engine.js -text
skills/er-relatorio-html/assets/uPlot.iife.min.js -text
skills/er-relatorio-html/assets/uPlot.min.css -text

# Cópia vendorizada byte a byte do uPlot v1.6.27 no novo skill de relatório
# (fatia 5B, item 5, Task 2) -- mesmo motivo: trava por teste de sha256
# contra a cópia legada acima (tests/test_relatorio_graficos.py).
skills/er-relatorio/assets/uPlot.iife.min.js -text
skills/er-relatorio/assets/uPlot.min.css -text

# Cópia congelada multiplos-justos v9.24 (byte a byte, sha256 em
# skills/er-multiplos-justos/manifest_vendor.json): nunca converter EOL nestes
# paths — qualquer conversão quebra o congelamento.
vendor/** -text

# Fixture de vetores de paridade (item 4, fatia A): gerada por
# skills/er-valuation/scripts/vetores_paridade.py com `newline="\n"` e
# comparada byte a byte contra uma regeração fresca em
# test_fixture_commitada_reproduz_o_gerador (tests/test_vetores_paridade.py).
# Sem "-text", um `core.autocrlf=true` local reescreve este arquivo em CRLF
# no próximo checkout/stash/clean e o teste passa a reprovar por diferença de
# fim de linha, não por regressão real — mesmo risco que os paths acima.
tests/fixtures/vetores_paridade.json -text

# Fixture de problemas de solver e de grade (item 4, fatia B): mesma receita
# (`vetores_solver.py`, `newline="\n"`) e mesmo risco de EOL da fixture da 4A
# acima. A regeração byte a byte é cobrada em
# test_paridade_solver_js.py::test_fixture_commitada_reproduz_o_gerador.
tests/fixtures/vetores_solver.json -text
